# This file is intentionally left empty.
# Conversation endpoints are served by the conversation_history core plugin
# via the @endpoint decorator (cat/core_plugins/conversation_history/endpoints.py).
# Do not add route handlers here to avoid duplicate registrations.
